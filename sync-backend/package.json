{
  "name": "tabulabili-sync-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "npx wrangler deploy --config wrangler.local.toml",
    "migrate:remote": "npx wrangler d1 migrations apply tabulabili-sync --remote --config wrangler.local.toml",
    "start": "node src/node-server.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^11.9.1"
  },
  "devDependencies": {}
}
